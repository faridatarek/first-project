import 'package:flutter/material.dart';
class MyAddition  extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Add to The list",
      style:TextStyle(fontSize: 44.0,color: Colors.teal),)
    );
  }
}